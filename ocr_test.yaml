name: MeowAgain
fws:
  - fw_id: 1
    name: "Fetch Docs"
    spec:
      debug: true
      docs_path: "s3://your-bucket/overall_concat_docs_dict.json"
      _tasks:
        - _fw_name: Fetch Docs

  - fw_id: 2
    name: "Extract Metadata"
    spec:
      debug: true
      output_path: "s3://your-bucket/dates_people_metadata.json"
      _tasks:
        - _fw_name: Extract Metadata

links:
  - [1, 2]
metadata: {}